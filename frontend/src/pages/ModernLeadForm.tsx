/**
 * Modern Lead Form Page - Wrapper for existing LeadForm
 */

import React from 'react';
import { LeadForm } from './Leads/LeadForm';

export function ModernLeadForm() {
  // Existing LeadForm already has modern UI components
  return <LeadForm />;
}
