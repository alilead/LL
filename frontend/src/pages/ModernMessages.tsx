/**
 * Modern Messages Page
 */

import React from 'react';
import { MessagesPage } from './Messages';

export function ModernMessages() {
  // For now, wrap the existing messages - can be fully redesigned later
  return <MessagesPage />;
}
